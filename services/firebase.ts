// Firebase service decommissioned. Moving to Supabase for improved live sync.
export const firebaseService = {
  pushState: async () => {},
  fetchState: async () => null,
  subscribeToChanges: () => () => {}
};